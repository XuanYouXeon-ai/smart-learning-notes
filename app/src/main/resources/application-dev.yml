spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    learning.notes: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
